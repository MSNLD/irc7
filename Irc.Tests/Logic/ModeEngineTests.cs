namespace Irc.Tests.Logic;

public class ModeEngineTests
{
    [SetUp]
    public void Setup()
    {
    }

    [Test]
    public void Test()
    {
        //var modeCollection = new Mock<IModeCollection>();
        //modeCollection.Setup(x => x.HasMode(It.IsAny<char>())).Returns(true);
        //modeCollection.Setup(x => x.GetModeChar(It.IsAny<char>())).Returns(0);

        //var dataStore = new Mock<IDataStore>();
        //var chatObject = new ChatObject(dataStore.Object);

        //var modeEngine = new ModeEngine(modeCollection.Object);
        //var result = modeEngine.Breakdown(chatObject, "+abc-def+g-h++--i+");

        //var expected = new List<ModeFrame>();
        //for (var a = 'a'; a <= 'i'; a++)
        //{
        //    expected.Add(new ModeFrame()
        //    {
        //        Mode = a,
        //        ModeValue = 0,
        //        ModeResult = ModeResult.NOACCESS
        //    });
        //}

        //System.Text.Json.JsonSerializer.Serialize(result).ShouldBe(
        //        System.Text.Json.JsonSerializer.Serialize(expected)
        //    );
    }
}